void perem()
{
	int a; // переменная задается для проверки условия
	int b; // переменная задается для проверки условия
	int c; // переменная задается для проверки условия
	int d; // переменная задается для проверки условия
}

int main{
printf
scanf
return111;
}